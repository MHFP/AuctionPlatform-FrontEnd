import { DateFutureDirective } from './date-future.directive';

describe('DateFutureDirective', () => {
  it('should create an instance', () => {
    const directive = new DateFutureDirective();
    expect(directive).toBeTruthy();
  });
});
